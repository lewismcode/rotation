# Railway/Nixpacks build for the worker service. ffmpeg is provided by the OS
# image (no fragile binary download at npm install time).
[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "npm run start --workspace @rotation/worker"
